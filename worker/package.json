{
  "name": "robot-fragrances-checkout",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that turns a Robot Fragrances cart into a Stripe Checkout Session.",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.mjs",
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put STRIPE_SECRET_KEY",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
