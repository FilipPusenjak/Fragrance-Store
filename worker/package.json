{
  "name": "robot-fragrances-checkout",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that turns a Robot Fragrances cart into a Stripe Checkout Session.",
  "type": "module",
  "scripts": {
    "pretest": "cd .. && node scripts/stamp-version.js",
    "test": "node --test test/*.mjs",
    "deploy": "cd .. && npx wrangler deploy",
    "dev": "cd .. && npx wrangler dev --port 8787",
    "secret": "cd .. && npx wrangler secret put STRIPE_SECRET_KEY",
    "tail": "cd .. && npx wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.118.0"
  }
}
